<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.comic.storebook.StoreBookDaoImpl">
	<insert id="addStoreBook"
		parameterType="com.project.comic.storebook.StoreBookDTO">
		insert into t_store_book(sidx,isbn13,isbn10,point,category,total,cnt)
		values(#{sidx},#{isbn13},#{isbn10},#{point},#{category},#{total},#{cnt})
	</insert>
	
	<select id="existIsbn" parameterType="java.util.Map" resultType="int">
		select count(*) from t_store_book where sidx=#{sidx} and 
		(isbn13=#{isbn} or isbn10=#{isbn})
	</select>
	
	<select id="existSbidx" parameterType="int" resultType="int">
		select count(*) from t_store_book where sbidx=#{sbidx}
	</select>
	
	<select id="getPageList" parameterType="java.util.Map"
	resultType="com.project.comic.storebook.StoreBookDTO">
		select * from t_store_book where sidx=#{sidx}
		limit #{start}, #{listsize}
	</select>
	
	<select id="getBooksCountAll" parameterType="int"
	resultType="int" >
		select count(*) from t_store_book where sidx=#{sdix}
	</select>
	
	<select id="getBook" parameterType="java.util.Map"
	resultType="com.project.comic.storebook.StoreBookDTO" >
		select * from t_store_book where sidx=#{sidx} and
		(isbn13=#{isbn} or isbn10=#{isbn})
	</select>
	
	<update id="updateBook" parameterType="com.project.comic.storebook.StoreBookDTO">
		update t_store_book set category=#{category},
														point=#{point},
														total=#{total},
														udate=now()
												where isbn13=#{isbn13} or isbn10=#{isbn10}
	</update>
	
	<delete id="deleteBook" parameterType="java.util.Map">
		delete from t_store_book where sidx=#{sidx} and (isbn13=#{isbn} or isbn10=#{isbn})
	</delete>

	<delete id="deleteSbidx" parameterType="int">
		delete from t_store_book where sbidx=#{sbidx}
	</delete>
		
</mapper>